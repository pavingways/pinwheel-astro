---
import { getEntry } from "astro:content";
import Config from "astro.config.mjs";
import Base from "@layouts/Base.astro";
import Banner from "@layouts/components/Banner.astro";
import HomeSections from "@layouts/components/HomeSections.astro";
import Cta from "@layouts/partials/Cta.astro";

const homepage = await getEntry("homepage", Astro.currentLocale ?? Config?.i18n?.defaultLocale ?? "de");
const { page, banner, lifecycle, audiences, why } = homepage?.data;
---

<Base title={page.title} description={page.description}>
  <Banner banner={banner} />
  <HomeSections lifecycle={lifecycle} audiences={audiences} why={why} />
  <Cta />
</Base>
